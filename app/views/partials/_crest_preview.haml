-if temp_image
  = image_tag "/#{temp_image}", :width => 500, :height => 500
-else
  = image_tag url_for(:controller => "crests", :action => "generate", :puzzlize => puzzlize, :rows => 6, :cols => 6, :crest_id => crest_id)

/ = link_to "Show", crest_path